services:
  - type: web
    name: chinese-review
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: SENDGRID_API_KEY
        sync: false
      - key: PUBLIC_URL
        sync: false
